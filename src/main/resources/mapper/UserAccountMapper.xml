<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 특정 EmployeeID으로 사용자 정보를 조회하는 쿼리 -->
<mapper namespace="com.hrm.dao.UserAccountDao">
<<<<<<< HEAD
    <select id="findByEmployeeId" resultType="com.hrm.dto.UserAccountDto">
        SELECT 
            UserID AS userId,
            EmployeeID AS employeeId,
            Username AS username,
            Password AS password,
            Role AS role
        FROM useraccounts
        WHERE EmployeeID = #{employeeId}
    </select>
    
       <!-- 프로필 업데이트 -->
    <update id="updateProfile" parameterType="com.hrm.dto.UserAccountDto">
        UPDATE employee
        SET 
            PhoneNumber = #{phoneNumber},
            Email = #{email},
            ProfileImage = #{profileImage}
        WHERE EmployeeID = #{employeeId}
    </update>

    <!-- 비밀번호 업데이트 -->
    <update id="updatePassword">
        UPDATE useraccounts
        SET Password = #{newPassword}
        WHERE EmployeeID = #{employeeId}
    </update>

    <!-- 사용자 존재 여부 확인 -->
    <select id="existsByEmployeeId" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM useraccounts
        WHERE EmployeeID = #{employeeId}
    </select>
    
    <!-- 비밀번호 확인 -->
    <select id="checkPassword" resultType="String">
        SELECT Password
        FROM useraccounts
        WHERE EmployeeID = #{employeeId}
    </select>
=======
	<select id="findByEmployeeId"
		resultType="com.hrm.dto.UserAccountDto">
		SELECT
		UserID AS userId,
		EmployeeID AS employeeId,
		Username AS username,
		Password AS password,
		Role AS role
		FROM useraccounts
		WHERE EmployeeID = #{employeeId}
	</select>

	<select id="findByEmail" resultType="com.hrm.dto.UserAccountDto">
		SELECT * FROM UserAccounts
		WHERE Username = #{email}
	</select>
>>>>>>> develop
</mapper>
